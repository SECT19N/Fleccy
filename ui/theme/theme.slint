// Fluent UI design tokens: color, typography, spacing, elevation, corner radius.
// Keep light/dark palettes here so components stay token-driven.

export global Theme {
    // out property <color> accent: #0067c0;
    // out property <length> corner-radius: 4px;
}

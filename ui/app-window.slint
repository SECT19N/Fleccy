// Root window. Imports the shell/navigation and composes the views.
import { Theme } from "theme/theme.slint";

export component AppWindow inherits Window {
    title: "Fleccy";
    // TODO: NavigationView shell + content views
}
